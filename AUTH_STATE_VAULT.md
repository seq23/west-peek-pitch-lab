# Authenticated Browser-State Vault

Encrypted canonical state: `~/AI_AUTH_VAULTS/west-peek-pitch-lab/playwright-storage-state.json.gpg`; disposable state: `.auth/playwright-storage-state.json`. It is gitignored, validated, never packaged, and required only for protected proof. Public-only states mark auth NOT APPLICABLE.

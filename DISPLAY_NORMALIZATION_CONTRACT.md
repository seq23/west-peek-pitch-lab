# Display Normalization Contract

All imported/provider/user content crosses one presentation boundary. Strip script/style/tags from default text, decode entities, normalize whitespace, summarize objects, clip inline content, wrap long tokens, and preserve raw source only in explicit details. Untrusted HTML requires reviewed sanitization. Hostile payloads must not hide actions or overflow pages.

#!/usr/bin/env node
import fs from 'node:fs/promises';import path from 'node:path';const source=process.env.CLICK_AUDIT_SUMMARY;if(!source)throw new Error('CLICK_AUDIT_SUMMARY required');const s=JSON.parse(await fs.readFile(source,'utf8'));const sev=s.results?.some(x=>/auth|tenant|data loss|corrupt/i.test(x.error||''))?'SEV-1':'STANDARD';const report={source,severity:sev,containment:sev==='SEV-1'?'ROLLBACK_OR_CONTAIN_BEFORE_REPAIR':'SOURCE_REMEDIATION_ALLOWED',productionFilesModified:false,requiredSequence:['patch source','release:self-heal','Hallmark','release:prepush','package','deploy','postdeploy audit']};const out=path.resolve('artifacts/diagnostics/postdeploy-remediation');await fs.mkdir(out,{recursive:true});await fs.writeFile(path.join(out,'plan.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));if(s.verdict==='PASS')process.exit(0);process.exit(2);

import assert from'node:assert/strict';import fs from'node:fs/promises';import{normalizeDisplayValue}from'../../src/runtime/normalizeDisplayValue.mjs';assert.equal(normalizeDisplayValue('<script>x</script><b>A &amp; B</b>').summary,'A & B');const c=JSON.parse(await fs.readFile(new URL('../fixtures/production-shaped-payloads.json',import.meta.url),'utf8'));for(const v of Object.values(c)){const r=normalizeDisplayValue(v,{maxLength:320});assert.equal(typeof r.summary,'string');assert.ok(r.summary.length<=320)}console.log('display normalization contracts: PASS');
